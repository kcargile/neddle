namespace Neddle.Caching
{
    /// <summary>
    /// Cache provider that uses a MemCached as its backing store.
    /// </summary>
    public class MemCachedCacheProvider : ICacheProvider
    {
    }
}
